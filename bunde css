/* Aligns the entire two-column row */
.two-column-row {
  display: flex;
  justify-content: flex-start;
  align-items: center;
  margin: 8px 0;
  width: 100%;
  max-width: 1000px;  /* or adjust as needed */
  margin-left: auto;
  margin-right: auto;
}

/* Label cell - fixed width */
.label-cell {
  width: 40%;
  text-align: right;
  padding-right: 12px;
  font-weight: 500;
  white-space: nowrap;
}

/* Input cell - flexible */
.input-cell {
  width: 60%;
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 6px;
}

/* Date wrapper */
.date-wrapper {
  display: flex;
  align-items: center;
  gap: 6px;
  width: 100%;
}

/* Calendar icon */
.calendarbutton {
  cursor: pointer;
  width: 20px;
  height: 20px;
  margin-left: 4px;
}

/* Amount and currency alignment */
.amount-wrapper {
  display: flex;
  gap: 10px;
  width: 100%;
  align-items: center;
}

.amount-wrapper .amount-input {
  flex: 1;
}

.amount-wrapper .currency-dropdown {
  width: 80px;
  min-width: 70px;
}

/* Optional: consistent input styling */
.input-field,
textarea,
select {
  padding: 5px 8px;
  width: 100%;
  box-sizing: border-box;
}