/* Container to center all two-column rows */
.row-container.two-column-row {
  display: flex;
  justify-content: center; /* centers entire row */
  width: 100%;
  margin: 10px auto;
  box-sizing: border-box;
}

/* Actual inner structure (left label, right field) */
.row-container.two-column-row > .label-cell {
  flex: 0 0 40%;
  max-width: 40%;
  padding-right: 10px;
  text-align: right;
  font-weight: 500;
  white-space: nowrap;
  box-sizing: border-box;
}

.row-container.two-column-row > .input-cell {
  flex: 0 0 50%;
  max-width: 50%;
  display: flex;
  align-items: center;
  gap: 8px;
  box-sizing: border-box;
}

/* Date wrapper */
.date-wrapper {
  display: flex;
  align-items: center;
  width: 100%;
  gap: 6px;
}

.calendarbutton {
  cursor: pointer;
  width: 20px;
  height: 20px;
  flex-shrink: 0;
}

/* Amount and currency wrapper */
.amount-wrapper {
  display: flex;
  width: 100%;
  align-items: center;
  gap: 10px;
}

.currency-dropdown {
  width: 90px;
  min-width: 80px;
}

.amount-input {
  flex: 1;
  min-width: 100px;
}

/* All inputs inside right cell */
.input-cell input,
.input-cell select,
.input-cell textarea {
  width: 100%;
  padding: 6px 8px;
  box-sizing: border-box;
}